{{!< main }}
{{> element/seo }}
<section class="text-gray-600 body-font">
  <div class="container px-5 py-12 mx-auto">
    <div class="flex flex-col mx-auto md:flex-row lg:w-4/5 gap-y-4 md:gap-y-0 md:gap-x-4">
      <img alt="{{ product.title }}"
          class="object-cover object-center w-full h-64 rounded md:w-1/2 lg:h-auto"
          src="{{ product.thumbnail }}"
      />
      {{> product/info }}
    </div>
  </div>
</section>