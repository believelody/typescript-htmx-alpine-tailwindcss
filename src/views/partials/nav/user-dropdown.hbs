<nav hx-boost="true" class="flex flex-col items-center justify-center">
    <a href="/users/me"
    hx-target="#content" hx-swap="innerHTML"
    aria-disabled="currentRoute.startsWith('/users/me')"
    disabled="currentRoute.startsWith('/users/me')"
    :class="currentRoute.startsWith('/users/me') && 'bg-gray-200'"
    class="px-4 py-2 text-center border md:w-full md:border-none"
    >
        My Profile
    </a>
    <a href="/users/me/settings" hx-target="#content" hx-swap="innerHTML"
        :class="currentRoute.startsWith('/user/me/settings') && 'bg-gray-800 text-white'"
        class="px-4 py-2 text-center border md:w-full md:border-none"
    >
        Settings
    </a>
</nav>
<div class="flex w-full">
    <button hx-post="/auth/logout" hx-push-url="true" hx-target="#content" hx-swap="innerHTML" class="py-2 mx-4 rounded link grow">Sign out</button>
</div>